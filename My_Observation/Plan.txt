
Пиши быстрее, но на том же уровне качества.

_______________
Порядок: 
10. GUI Сделать генерацию меню в зависимости от входных данных. Мб, создать новую таблицу в БД.
0. Вроде как - вроде как. Структура зимплементирована. Сейчас перекидываю силы на создания ГУИ. Возможной некий функционал ещё нужно будет добавить.
Обязательно заимплементировать функциональность используемых Класс в примере ГУИ.



1.3; Сделать метод получение Клиентов/Работников в зависимости от филтра. Или, чтобы через  Enum был поиск.
1.2; Сделать редиректинг с index.html на Home/..
1.3; Реалищовать функицонал для имплиминтирующихся клас.
2.0 Настроить static fields. ?????????????????

3. ГУИ.
++++++++++++++++____________++++++++++++++++++:
TO DO: 
Может View сжырает? И остовляет лишь то, что будт отображаться? Поэтому и следует передавать id? НЕ. 
Следует правильно данные выводить и всё будет пучком.
________________________
Next git commit message:

Add some set/get with Object Typee Filter.
______________________________________
Примечание:
0.5 Проверять  
0.4 Дошло только сейчас. А Person ведь многоаспектный Obj.
0. При добавлении нового obj в БД, сохраняется ли двунаправленность?
0.1 Истинная ли двунаправленность и ровны ли эти obj?

1. В базе данных только зранение - никакой логики.
2. Написаная мною комментарии к методу должны быть короткие в одну строку ( лучше всего). Примем:  toString() - возвращает описание объекта.
As additional:
1. Create Not-Found web-page.
7.  Мб с массива obj имзменить на IEnumerable<Тип_модели>. Чтобы иметь доступ к ним и возможность редактировать без дополнительного поиска.
________________
Понять:
2. Не будут ли мешать ограничения на Get{} работе моделе? Мб, ошибки будут.
1.0; Может реализовать следующим образом? public virtual DbSet<Employee> Employees { get { People.ToList().Where(e => e.RelationShipWithCompany = "Employee") } set; } /// TO DO Also add string into Enum or josn-parameters/
1. Если со стороны кода написать DBSet<Starzysta>, но не маппировать его на  БД, то можно ли им пользоваьтся как ползуемся другими DbSet'ami? 
1.1; Можно ли использовать свой дискриминатор с другим названием? 
_____________
Понято:
0. А как сделать автоаматическое доккументирование? Или мне ещё комментарии нужно будет писать? || Писать только там где оч. нужно.
0.2. Ассоциации должны быть двунаправленные!!!  || ORM создает двунаправленность.
0.1 Вот как Я вччера дигаграммы создавал разные для разных моделей, вот так и в коде следует сделать. Отдельно для БД реализация и отдельно для С# || ORM всё делает.
__________
ИНТЕРЕСНЕНЬКОЕ:
1. Создал view для клиента. Смаппировал БД в C# -> Создал obj "Client". Круто! Но, к сожалению, в Базе данных только хранение данных и никакой логики. 
___________
Current task:
8. Реализовать "find receptionist" в Home. 
8.1 в остальных местах - убрать эту кнопку.
1. GUI
5.5 Сделать partialView для Таблицы Client list, который будет использоваться в Home, ClientList и в Details к некоторым Objs. Мб, нужно будет создать objViewModel  для Select( можно подсмотреть с Metanit). 
        
5. Настроить текст-сообщение внутри/в центре и переходить к созданию    Home св двумя с таблицами.

1.2 partial view для таблицы?
2. ИсапользИспользовать шаблоны длясоздания View модели. " Типы шаблонов представления"
 Сегодня Я должен всё закончить.
6. При создании Forms сделать возможность назначить существующего Клиента -> Рецпционистом и наоборот. Мб, добвыить кнопку, мол, создай и выбирай.   
Там где Enum  и поднобное -> use <select>
Поряжок: 
3. Forms
 
___________________________---
Не реализоывно, но возможно понадобится:
1. Проверка, перед сменой ставки... Сделал, но хз, насклько хорошо сделал.
2. Смена типа опыта с Apprentice на Experienced, после работы в отеле определенное количесвто времени. !сДЕЛАл 
4. Пройтись по всем Get/set и создать для них соответствующие ограничения ( прямо как в примере ).
3. wyliczenie stawkę za godzine pracy() <- Чтобы зависило от количества/качества свойств.  И типе obj
4. Удаление записей каждое n-время( Смотри на диаграмме)
1. Сначала структуру сделать.
1.1 Мб, создать Unit Test(хотя лучше без него).

6. Настролить адаптативность под мобилки и Так далее.
________________
lINKS:
localhost:33189/Home/Clients/


____________
Если будет выбивать ошибку глянь :
1. В Person методы создания oBJ РАЗНОГО тиПА, МОЖЕТ Я напутал порядок параметров.
2. в Person - созда кучу ограничей доступа к полям, может из-за этого не может полноценно работать Entity Framewwork.
3. Хз насколько это правильно, но если что, то можно будет ставиьт для МОдели для БД, как иони и должны быть смаппированы. А с "фильтрацией" по типуObj закинуть в другой файл
4. А в каждом ли методе контроллера переадется инфа о названии страницы? (Скорее нужно в опреджеленных, но всё же, проверяй).
5. Этот код нк мешает в созданных мною контроллерах?
[ResponseCache(Duration = 0, Location = ResponseCacheLocation.None, NoStore = true)] 
        public IActionResult Error()
        {
            return View(new ErrorViewModel { RequestId = Activity.Current?.Id ?? HttpContext.TraceIdentifier });
        }

 _________________________________-
/                                                                                                                               \
Если что-то не раоботает, а должно:
1. CSS + HTML? Соответсвуют ли азвания класс и названия в коде CSS? Может напутал?
2. Закрой браузер и запусти снова, тогда кэш очиститься, и будет выводить css.

___________________
For realization:
+_+_+_+_+_+_+_+__+
Перед сдачей:
1. Убрать Метод Clinets() in HomeController.


______________________________
прИМЕРЫ работающего кода.
dataBaseService.AddCustomerConversation(
                new CustomerConversation
                {
                    IdCustomerConversation = 1 + dataBaseService.GetMaxIdCustomerConversation(),
                    ClientIdOsoba = 1,
                    ClientIdOsobaNavigation = dataBaseService.GetPersonById(1),
                    EmployeIdOsoba = 11,
                    EmployeIdOsobaNavigation = dataBaseService.GetPersonById(11),
                    MarkServiceQuality = 4,
                    StartDateTime = new DateTime(2021, 06, 14, 5, 10, 20),
                    EndDateTime = new DateTime(2021, 06, 14, 15, 10, 20),
                    ConversationDurationInSeconds = 600
                }
            );
________________
Completed tasks:
1. Add Auto-generation library anad set up it. + 
1.2. Похоже, что нужно будет изменить структуру в C# коде, так как она не соответсвует тому что должно быть написано. Значит, забивая ( почти) на БД и думаю, как эта часть должна быть рпеализована/сммапирована с UML to C#. 
1.3. Сделать overlapping для Client и Employee. Это означает о конце, проверочной имплиминтации и выбора вектора. Помни, главное, чтобы работало, а остальное - то всё хуйня.
2. Create overlapping Client and Employee
1.4 Настроить Enum.
2.0 Создать AddLanguage(Employee, LanguageAttributes) в Service и использовать в Person.
2.1 Проверить яаляется ли Person -> Employee.
2.2 Не будет ли после добваления к нему приписано языков больше чем три. 
2.1 Сммапировать ещё раз.
1.4.6 Добавить проверку contains() in Enum list.
1.4.2 Л/// TO DO: ДОбавить: 1. статические поля; 2. Как статическое поле сделать ограничение количества последних мест работы.
1.4.1 Enum: Добавить к "EnumMangar'aм" GetEnumFromString(string stingValue)?
1.5 Размер сделанной структуры: ./Images/competed_sturcture.png

1.4.5 Добавить проверку в set() для enum в Person; 

1.4.3 Добавить в set проверку на тип obj, мол если ты клиент, то тебе нельзя поставить один из ТиповРаботника.

3. Добавить Enum в Person.
3.1 Создать полноценную структуру: wilodziedziczenie, наследования,  overlapping(готов).
3.2. Добавить интерфейс. Или вместо него, реализовывать метод (для Уборщик-Рецепшионист), который будет проверять, кем являеется Obj -> соответствующий метод.
5.0 Сделать Menu BAR, но для Register.

Нафиг часть з БД, сделаю как юыло у Драбика. В одном кусочке, и через id проверка чё за страница - соответчвенное отображение. / План реализации динамического меню: 1. Создаю 2 енци в БД. 1-ая MenuBarItem; 2 WebPageName. 3. KlassAssocycyj между этими двумя классами.  +
 | 2. Маппирую в tmpModels, откуда извлекаю настройки для выше перечисленных энций. +
 |3.  Создаю отдельный PartialView by template-model.
 \4. Добавляю звено рендеринга, где нужно.
 +++_____________++++++++++ Opis GUI:
 1. NavMenuBar 3 типа: 1_main (Home, Register new element, Show elements); 2_show (Show Receptionist' Show Conversation 'Show Orders', 'Show Offers'); 3_register ( 'Register Receptionist'  'Register Conversation'  'Register Orders' 'Register Offers')

_-2313 Доделать кнопки и идти дальше.

Offer -> Order
Изменить БД, чтобы customerConv имел свой PRimaryKey.
4. От нуля буду писать CSS. Полностью, чтобы ничего не мешало.  И перепроверять на момент валидности ( через соответсвующий сайт).
3. Ехё нужен контроллер.
        4. Реализовать для кнопок в таблице CSS стиль.
5.1 Реализовать таблиц через partialView.
1. Списки + 
2.0 Сделать partial лист отдельно для HomeList, меньше колонок в таблицах и тогда поместяться 2. Если нет, то убрать пару кнопок. +
2. HomeList + logic(во время создании поменять в View с Id на Obj.);  +